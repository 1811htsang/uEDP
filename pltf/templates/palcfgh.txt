//ANCHOR - PAL configuration header
#ifndef __PAL_CFG_H__
  #define __PAL_CFG_H__

  /** ANCHOR - Khai báo các cấu hình PAL
   * @example
   *    #define LOGDP_MAX_OUTPUT_FN  (8u) // Số lượng hàm callback tối đa có thể đăng ký để xuất dữ liệu log
   * @attention Xin đừng sửa đổi, tự động sinh bởi Kconfiglib
   */
  
  {% for config in pal_configs %}#define {{config}}
  {% endfor %}
#endif //__PAL_CFG_H__
